ΒΙΒΛΙΟ ΕΚΡΗΚΤΙΚΩΝ ΥΛΩΝ (EXPVAULT) — CHECKLIST ΓΙΑ MERGE v2 → main
================================================================

Αυτό είναι **διαφορετικό** από το [`RELEASE-CHECKLIST.md`](RELEASE-CHECKLIST.md) — εκείνο
καλύπτει τα συνηθισμένα patch releases του v1 (main). Αυτό εδώ καλύπτει το γεγονός που θα
συμβεί ΜΙΑ φορά όταν το `v2` ωριμάσει αρκετά ώστε να γίνει η νέα κύρια γραμμή.

## Γιατί δεν είναι απλό `git merge`

Το `main` δέχεται patch releases (π.χ. v1.1.12) ασύγχρονα με το `v2` — μπορεί να έχει
προχωρήσει commits που δεν υπάρχουν στο `v2` (π.χ. το backport του `_clean_parst` fix, ήδη
έγινε cherry-pick χειροκίνητα και στα δύο, βλ. `DONE-v2.md`). Πριν το merge:

1. `git log main..v2 --oneline` και `git log v2..main --oneline` — δες τι έχει το καθένα που
   δεν έχει το άλλο. Οτιδήποτε στο `main` που λείπει από το `v2` (π.χ. μελλοντικά maintenance
   fixes μετά το v1.1.12) πρέπει να μπει στο `v2` πρώτα (merge `main` → `v2`, όχι cherry-pick
   ένα-ένα — απλούστερο όταν δεν υπάρχει επικάλυψη).
2. Έλεγξε αν χρειάζεται migration στο `backend/database.py` (`ALTER`/`CREATE TABLE` που δεν
   υπάρχει στο `main`) — αν ναι, το `allowed-versions.json`'s `safeDowngradeFloor` πρέπει να
   μηδενιστεί στη νέα έκδοση (βλ. πολιτική στο κύριο checklist).
3. `.\build.ps1` πάνω στο `v2` πριν το merge — πρέπει να περάσει καθαρά πριν καν σκεφτείς merge.

## Βήματα

1. Merge `v2` → `main` (`git checkout main && git merge v2`).
2. Ενοποίηση αρχείων -v2: το περιεχόμενο του `README-v2.md`/`ΟΔΗΓΟΣ_ΧΡΗΣΗΣ-v2.md`/
   `VERSIONS-v2.md`/`DONE-v2.md`/`TODO-v2.md` μπαίνει μέσα στα βασικά αρχεία
   (`README.md`/`ΟΔΗΓΟΣ_ΧΡΗΣΗΣ.md`/`VERSIONS.md`/`DONE.md`/`TODO.md`) — δεν μένουν σαν
   ξεχωριστά αρχεία μετά το merge, ήταν προσωρινά μόνο όσο το `v2` ήταν branch.
   Διάγραψε τα `-v2` αρχεία μετά την ενοποίηση.
3. Απόφαση αριθμού νέας έκδοσης: αν το `v2` δεν πρόσθεσε schema αλλαγή, μπορεί να συνεχίσει
   τη σειρά `1.1.x`/`1.2.0` — αν πρόσθεσε, πιθανό `2.0.0` (major bump), ανάλογα με το πόσο
   μεγάλη είναι η αλλαγή. Απόφαση χειριστή, όχι αυτόματη.
4. Από κει και πέρα: ίδια βήματα με το κύριο `RELEASE-CHECKLIST.md` (VERSIONS.md entry,
   allowed-versions.json, package.json bump, build, GitHub release).

   **Πρόσεξε το `allowed-versions.json` συγκεκριμένα**: είναι το ίδιο αρχείο που ήδη
   fetch-άρουν οι σημερινοί v1.x εγκατεστημένοι χρήστες (hardcoded path στο `version-check.js`,
   βλ. CLAUDE.md). Αν το άλμα έκδοσης είναι μεγάλο (π.χ. 1.1.x → 2.0.0) ή η λογική/σχήμα του
   manifest άλλαξε ουσιωδώς, εξέτασε το μοτίβο που χρησιμοποιεί το lab-galatista: ξεχωριστό
   `allowed-versions-v2.json` για τους νέους χρήστες, με το παλιό `allowed-versions.json` να
   μένει αμετάβλητο ώστε οι ήδη εγκατεστημένοι v1.x να μην επηρεαστούν από συστάσεις/σχήμα της
   νέας γραμμής (`modules/update-check.js` εκεί, σχόλιο στην κορυφή του αρχείου). Αν το άλμα
   έκδοσης είναι μικρό και το σχήμα ίδιο, μπορεί να αρκεί απλή ενημέρωση του υπάρχοντος αρχείου
   — απόφαση χειριστή τη στιγμή του merge.
5. Μετά το merge, το `v2` branch μπορεί να διαγραφεί (`git push origin --delete v2`) ή να
   ξαναρχίσει από το νέο `main` για την επόμενη γενιά ανάπτυξης — απόφαση χειριστή τότε.

## Αν χρειαστεί νέο branch για την επόμενη γενιά ανάπτυξης

Ίδιο μοτίβο: `git push origin main:v3` (ή όποιο όνομα), όπως έγινε αρχικά για το `v2` από το
`main` στο v1.1.11 (fast-forward, καμία απώλεια αφού το `main` προηγείται πάντα).
